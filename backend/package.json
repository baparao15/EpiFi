{
  "name": "fair-split-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Fair Split: bill photo + description -> reconciled per-person breakdown",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
